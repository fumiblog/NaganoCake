<div class="container">
  <div class="row">
    <div class="col-md-4">
      <h3>ジャンル検索</h3>
      <table class="table table-striped">
        <tr>
          <th>ジャンル</th>
        </tr>
        <% @genres.each do |genre| %>
        <tr>
          <td><%= link_to genre.name, public_items_path %></td>
        </tr>
        <% end %>
      </table>
    </div>
    <div class="col-md-8">
      <h3></h3>
      <div class="cakeimage"></br></br></br>
        <div class="overview">
          ようこそ、NaganoCakeへ！</br>
          このサイトは、ケーキ販売のECサイトになります。</br>
          会員でない方も商品の閲覧はできますが、</br>
          購入には会員登録が必要になります。
        </div>
      </div>
      <div class="container">
        <% @order_details.each do |order| %>
        <div class="row row-cols-1 row-cols-md3 g-4">
          <div class="col">
            <div class="card h-100">
              <%= attachment_image_tag order.item, :image, format: 'png', fallback: "no_image.jpg", class: "card-img-top", size: "200x200" %>
              <div class="card-body">
                <h4 class="card-title"><%= order.item.name %></h4>
                <p class="card-text"><%= order.item.introduction %></p>
              </div>
              <div class="card-footer">
                <div class="text-muted"><%= (order.item.price*1.1).floor %>円</div>
              </div>
            </div>
          </div>
        </div>
        <% end %>
      </div>
  </div>
</div>
